import 'package:flutter/material.dart';

class HotOffersView extends StatelessWidget {
  const HotOffersView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Hot Offers View")));
  }
}
